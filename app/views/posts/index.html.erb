
<div class="contents">
<div class="message-box-wrap">
  <% @posts.each do |post| %>
  <div class="message-box">
    <div class="message-top">
      <span class="number"><%= post.id %>.</span>
      <span class="title"><%= post.title %></span>
      <div class="message-top-right">
        <span class="genre"><%= post.genre.name %></span>
        <span class="name"><%= post.name %></span>
      </div>
    </div>
    <div class="message-bottom">
      <span class="content">詳 細:<%= post.content %></span>
      <div class="message-bottom-under">
        <span class="button"><%= link_to "削除", post_path(post.id), method: :delete %></span>
        <span class="button"><%= link_to "編集", edit_post_path(post.id)%></span>
        <span class="date"><%= post.created_at.to_s(:datetime_jp)%></span>
      </div>
    </div>
  </div>
  <% end %>
</div>
</div>